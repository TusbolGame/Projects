<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="MyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="OnlyOneTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="MyDialogTheme" parent="ThemeOverlay.AppCompat.Dialog" />

    <style name="MyProgressDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">

        <item name="android:textColor">@android:color/black</item>
        <item name="android:background">@android:color/white</item>
        <item name="colorAccent">@android:color/holo_blue_dark</item>
    </style>

    <style name="CropImageActivityTheme" parent="Theme.AppCompat.DayNight">
        <item name="toolbarStyle">@style/CustomActionBarStyle</item>
        <item name="android:windowBackground">@color/colorBlackBright</item>
        <item name="actionMenuTextColor">@color/colorWhite</item>
        <item name="android:homeAsUpIndicator">@null</item>
    </style>

    <style name="CustomActionBarStyle" parent="@android:style/Theme.Holo.Light">
        <item name="android:background">@color/colorBlackBright</item>
    </style>

</resources>
